# 成果物

## プルリクエスト

main ブランチから開始する実装の完了条件は、以下のプルリクエストが作成されていることとする。

- クレート単位の実装ブランチから、リリース候補ブランチへ向けたプルリクエスト
- すべての実装のプルリクエストがマージされたリリース候補ブランチから、main ブランチへ向けたプルリクエスト（リリース PR）

ブランチの名前は、リリース候補ブランチを `rc/<リリースするバージョン>`、クレートの実装ブランチを `feature/<クレート名>` とする。クレートに属さない変更の実装ブランチは、「コミットメッセージ」の type を用いて `<type>/<変更の内容>` とする。

実装とプルリクエストの記述の内容の間に矛盾がないこと。

## チャットの回答

- 日本語以外の言語で見られる擬人化・擬態語・イディオムの表現を、逐語訳して日本語として使わないこと。
- 直後に説明される内容を先出しする軽い言い回しを日本語で使わないこと。
- 推測や感想を表現しないこと。

# コミットの前提条件

以下のすべての条件を満たすまでコミットを行わないこと。満たされない条件がある場合は、修正してから再度検証すること。

## 検査の通過

以下のコマンドがすべて正常終了すること（[CI](./.github/workflows/ci.yml) と同等）。

- フォーマット: `make format`（適用後に差分が生じないこと）
- 静的解析: `make lint`
- 自動テスト: `make test`
- ビルド: `make check`

## 実装とドキュメントの一致

- 実装と [README](./README.md) の記述の内容の間に差異がないこと。
- README にアプリケーションの仕様のすべてを記述しないこと。仕様はテストで担保すること。

## 依存関係の管理

依存するサードパーティのライブラリはすべてワークスペースの [Cargo.toml](./Cargo.toml) に記述し、サブパッケージ（クレート）の Cargo.toml はワークスペースの設定を参照すること。アプリケーション本体のクレートはサードパーティのライブラリを直接使用せず、汎用クレートを介して利用すること。

## GitHub Actions の記述

ワークフローや composite action が使用する action は、`uses` にコミットハッシュを必ず指定し、そのハッシュが指すタグ名を `# v7.0.0` の形式のコメントで併記すること。

## クレートの構成

アプリケーションは、個性のある複数のパッケージとそれを繋ぐ実装の集積とする。クレートは単体でパッケージとして明確な目的を持つ実装の単位であり、以下の 2 種類に分類する。

- **汎用クレート** — 本アプリケーション専用ではない、汎用性のあるパッケージ。単一の明確な目的の下に実装し、複数の目的を併せ持つクレートを作成してはならない。目的は各クレートの Cargo.toml の `description` に記述する。汎用クレートには `app`・`ui` のような複数の層を導入しない。
- **アプリケーション本体のクレート**（`crates/map`）— 汎用クレートを繋ぎ合わせてアプリケーションの機能を実装する唯一の例外的なクレート。

ソースコードはできる限り汎用クレートへ切り出し、アプリケーション本体のクレートは小さく保つこと。アプリケーションのドメイン概念や保存形式に依らない計算は、すべて抽象化して汎用クレートとして実装すること。アプリケーション本体のクレートには責務ごとのレイヤー（ディレクトリによる層別）を導入せず、汎用クレートを繋ぐ薄い実装のみを置くこと。

汎用クレートの一覧はワークスペースの [Cargo.toml](./Cargo.toml) の `members` で、各クレートの目的は各クレートの Cargo.toml の `description` で管理する。本ファイルへ一覧の写しを記述しないこと。

### 抽象化の位置づけ

アプリケーション本体の実装のために計算を抽象化して汎用クレートへ切り出すことは、オーバーエンジニアリングではない。オーバーエンジニアリングとは、現在の必要を超えた機能や一般性を予測で作り込むことを指す。ここでの抽象化は機能を足す行為ではなく、アプリケーション本体が現に必要としている計算からドメインの語彙と結合を取り除き、実装の境界を確定させる行為である。

この抽象化は以下の効果によって正当化される。

- 切り出された計算は、ドメインの文脈を経由せずに、クレートの公開機能への統合テストとして単体で検証できる。
- アプリケーション本体の変更理由（ドメインの変化）と汎用クレートの変更理由（計算の変化）が分離され、変更時に触れる場所が確定する。
- アプリケーション本体に残るコードはドメインの記述に純化され、アプリケーションの仕様が読み取りやすくなる。
- 「このアプリケーション以外でも使えるか」という問いが、境界を正しく引けているかの検算として機能する。再利用の実現そのものは目的ではない。

一方で、以下はオーバーエンジニアリングにあたるため行わないこと。

- アプリケーション本体が使わない機能・引数・型を、将来の予測で汎用クレートへ加えること。一般性はドメインの語彙の除去と型引数化によって得ること。
- 語彙の除去と型引数化で足りる場面で、それを超える機構（トレイト階層・プラグイン機構など）を導入すること。

## 責務の分離

- 各関数について、入力・出力・入力から出力への変換規則が、型シグネチャと実装から読み取れること。
- 1 ファイルからエクスポートする型・関数は 1 つまでとすること。ファイル内部でのみ使用する補助関数は認めない。型に属さない定数は専用のファイルに分離する。
- 関数本体には、入出力の対応関係を表現するのに必要な抽象度の処理のみを記述すること。より低い抽象度の処理は別の関数へ委譲すること。
- ディレクトリは責務の抽象度ごとに分けること。処理全体を組み立てる関数と、それが委譲する低い抽象度の関数を、同一ディレクトリに置かないこと。

## コードの体裁

- 関数本体の処理は、意味のまとまりごとに段落へ分け、段落の間に空行を置くこと。
- 1 つの段落には 1 つのまとまった処理のみを含めること。

## 日本語の文章

コメント・ドキュメントなど日本語の文章では、段落の途中で改行しないこと。1 つの段落は 1 行で書き、折り返しは表示側に委ねる。改行は段落の区切りとしてのみ用いること。

## コメントの規約

コメントはすべて日本語で書くこと。

### 記述してよい内容

現在のコードから読み取れない情報のみを書くこと。以下に限る。

| 内容                                         | 補足                                                 |
| -------------------------------------------- | ---------------------------------------------------- |
| 現在の実装がその形である理由                 | 結論のみを書き、判断の過程や検討の履歴は書かないこと |
| 外部の制約への参照                           | 仕様書・不具合報告・外部システムの挙動など           |
| 一見して誤りに見えるが意図的である処理の説明 |                                                      |

### 記述してはならない内容

| 内容                       | 例・理由                                               |
| -------------------------- | ------------------------------------------------------ |
| コードから読み取れる内容   | 処理の再記述、変数名・関数名の言い換え                 |
| 変更の経緯                 | 以前の実装への言及、変更前後の比較、削除した処理の説明 |
| 作業の経過                 | `// 修正した`、`// ここを変更` など                    |
| 未対応の課題               | `TODO:` など。課題は必ず解決すること                   |
| コーディング規約           | 規約は本ファイルで管理する。個々のコードに転記しない   |
| コメントアウトされたコード |                                                        |

### ドキュメンテーションコメント

エクスポートする関数・型・定数には、ドキュメンテーションコメント（`///` で始まる、言語処理系やエディタが認識する形式のコメント）を付けること。内容は以下に限る。

| 対象 | 内容                             |
| ---- | -------------------------------- |
| 関数 | 入出力の意味と事前条件・事後条件 |
| 型   | その型が表現する概念             |
| 定数 | その値が意味するもの             |

## コミットメッセージ

以下の形式に従うこと。本文は含めない。

```
<type>: <subject>
```

### type

以下のいずれかを指定すること。

| type       | 用途                                             |
| ---------- | ------------------------------------------------ |
| `feat`     | 機能の追加・変更                                 |
| `refactor` | 外部から観測可能な振る舞いを変えないコードの変更 |
| `test`     | テストの追加・修正                               |
| `docs`     | ドキュメント・コメントのみの変更                 |
| `build`    | ビルド設定・依存関係の変更                       |
| `ci`       | CI 設定の変更                                    |
| `chore`    | 上記のいずれにも該当しない変更                   |

### subject

- 変更内容を 50 文字以内で記述すること。
- 末尾に句点を付けないこと。
- 日本語で書くこと。

### コミットの単位

- 1 コミットには 1 つの関心事のみを含めること。無関係な変更を同一コミットに含めないこと。
- 各コミットが「検査の通過」の条件を満たす状態であること。

## テストコードの規約

### 配置

- 汎用クレートでは、ファイルごとの単体テストを設けないこと。汎用クレートのテストはすべて、クレートのディレクトリの `tests/` に置く統合テストとして、クレートが公開する機能を検証すること。例外として、unsafe なコードの単体テストは型と実装の整合性を保証するために必須とする。
- アプリケーション本体のクレートには、テストを必要とする計算を置かないこと。テストを必要とする計算は汎用クレートへ切り出し、そのクレートのテストとして検証すること。
- ドキュメンテーションコメント内のコード例（doctest）は、使用例の提示を目的とする場合にのみ記述すること。網羅的な検証は統合テストで行うこと。

### 依存規則

- テストでのみ使用する依存クレートは `[dev-dependencies]` に記載すること。

### 適用しない規則

`tests/` 配下には、「責務の分離」の以下の項目を適用しない。

| 項目                         | テストコードでの扱い                       |
| ---------------------------- | ------------------------------------------ |
| 1 ファイル 1 エクスポート    | 適用しない。テスト関数を複数記述してよい   |
| 補助関数の禁止               | テストデータの構築などの補助関数を認める   |
| ドキュメンテーションコメント | 不要。テスト関数名で検証内容を表現すること |

### 適用する規則

- 「コメントの規約」はテストコードにも適用する。
- 「検査の通過」はテストコードを含めて満たすこと。

### テストの内容

- テスト関数名には、検証する振る舞いを表す名前を付けること。
- 1 つのテスト関数では 1 つの振る舞いのみを検証すること。
- テストは実行順序に依存しないこと（`cargo test` は既定で並列実行されるため）。
